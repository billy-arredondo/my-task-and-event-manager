# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db

# TypeScript cache
*.tsbuildinfo

# Vite cache
.vite/

# skills.sh / Claude Code agent skills
# Downloaded skill content — regenerado con `skills install` usando skills-lock.json
# Tratar como node_modules: no versionar, sí skills-lock.json
.agents/
.claude/skills/
.commandcode/skills/

# Claude Code — configuración local de la máquina (nunca compartir)
.claude/settings.local.json
# Claude Code — worktrees temporales de agentes
.claude/worktrees/

# Supabase — archivos temporales del CLI (generados al vincular el proyecto)
supabase/.temp/
